import {
  EditorContent,
  useEditor,
  BubbleMenu,
  FloatingMenu,
} from '@tiptap/react';
import StarterKit from '@tiptap/starter-kit';

const TipTap = () => {
  return <>editor</>;
};

export default TipTap;
